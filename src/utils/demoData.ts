/**
 * demoData.ts — intentionally empty.
 * No pre-seeded locations or fake users.
 * All data is created by real users through the app.
 */
export const initializeDemoData = async (): Promise<void> => {
  // nothing to do
};
